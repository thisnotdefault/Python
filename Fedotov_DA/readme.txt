1. На уроке все объяснили, не сложно

3. В задаче указано"возвращать кроме курса дату,которая передаётся в ответе сервера."
Я так понял что речь идет о объекте "Date" который передается в заголовках ответа сервера, c которым в последствии мы ведем работу,
а дата 11.03.2021 на сайте указана специально чтобы сбить с толку, потому что для "*" слишком просто показалось просто так же отформатировать строку: -
"<ValCurs Date="11.03.2021" name="Foreign Currency Market">" ибо особого труда это не составит.
А вот с ответом сервера "10 MAR 2021" пришлось поработать с объектом Date тут уже интересно, чтобы преобразовать в 2021-03-10 пришлось разбираться (так как отдает месяц не числом!)
Да, схалявил на replace(), которое несколько раз использовалось к одной строке, хотя была мысль написать lambda функцию для этого ,
но по времени решил лучше разобраться с decimal и datetime. Для себя решил красиво распарсить по словарю значения пример: USD {nominal: 1, value:.. } и т.д.
В целом сложность была с decimal и конвертацией дат.
Но если почитать , то все это выполнимо!

5. В 4 задании "Убедиться, что ничего лишнего не происходит." я так понял что нужно показать что усвоил про конструкцию if __name__ == "__main__":.
С аргументами пришлось покрутить , но в целом выполнимо!
Сначала хотел сделать внутри функции return print(currency_rates(*args)) и назвать функцию show_currency_rates, но вспомнил фразу принты долой,
второй вариант думал сделать две строки print(currency_rates(*args)) return (currency_rates(*args)) в итоге решил лучше так не делать , просто
добавил print функции в конце скрипта.

Также Pycharm на Windows ругался на дублирование кода в двух файлах, что забавно на Linux этого не было.(может то что pycharm лицензия, а на линуксе коммерция)
Но так как это было в рамках нашего практического задания не стал трогать.

В целом практика была интереснее, но уже полегче прошлых практик показалась.


