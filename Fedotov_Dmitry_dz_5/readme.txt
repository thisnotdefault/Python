1. Не сложно
2. Сделал и 1 и 2(*) задачу, потому что во втором не уверен правильно ли я сделал.
На уроке вы приводили пример:

MAX_NUM = 10 ** 7
def nums_gen():
    for num in range(1, MAX_NUM + 1):
        if not num % 7:
            yield num

    Пытался также реализовать для практической краткую запись if not num % 2:
    пример :

    MAX_NUM = 15
    nums = (num for num in range(1, MAX_NUM + 1) if not num % 2) # дает четные!

    А по логике должен был давать нечетные. Начал разбираться.
    В итоге чтобы были нечетные нужно убирать "not"!
    Можно запутаться)
    Поэтому в решении практической чтобы себя не путать не использовал краткую запись а добавил != 0.
    Я так понял проблема в том что булевое 0 это False , 1 это True в python, а "not" меняет булевое значение.
    Так как остаток у нас может быть 0 или единица , то условие if not num % 2 работает от обратного ?
    Правильно ?

3. Прилично помучился с None.
4. Потратил немного времяни чтобы разобраться с list comprehension.
5. Если писать не правильно (решение с count) , то быстро , а вот чтобы переписать более оптимизированно и правильно
долго мучился. (Проблема была в том , что долго не мог допереть как сделать порядок, так как сет неупорядочный.
